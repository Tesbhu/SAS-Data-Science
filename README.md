# SAS
SAS es el acrónimo de "Statistical Analysis System" (Sistema de Análisis Estadístico, en español). Es un software ampliamente utilizado en el campo de la estadística y la analítica de datos. Fue desarrollado por SAS Institute Inc., una empresa de software con sede en Estados Unidos.

SAS proporciona un conjunto de herramientas y funcionalidades que permiten realizar diversas tareas relacionadas con el análisis de datos, la minería de datos, la gestión de bases de datos y la generación de informes. Es utilizado por organizaciones de diferentes industrias, como empresas, instituciones académicas, agencias gubernamentales y centros de investigación.

El software SAS ofrece una interfaz gráfica y un lenguaje de programación propio, conocido como SAS Language, que permite a los usuarios realizar análisis estadísticos, manipular y transformar datos, crear modelos predictivos, generar gráficos y realizar otras tareas de análisis avanzado.

SAS es conocido por su capacidad para manejar grandes volúmenes de datos y su amplia gama de técnicas estadísticas y algoritmos de modelado. También es reconocido por su robustez, seguridad y flexibilidad.

Es importante destacar que SAS es una herramienta comercial y requiere una licencia para su uso. Además del software, SAS Institute también ofrece servicios de capacitación y soporte técnico para ayudar a los usuarios a aprovechar al máximo sus capacidades.

## ¿Porque muchas compañias solicitan saber SAS en su ofertas de trabajo?
SAS es ampliamente utilizado por grandes compañías y es solicitado en trabajos de analista de datos por varias razones:

1. Capacidad de manejar grandes volúmenes de datos: SAS es conocido por su capacidad para manejar eficientemente grandes cantidades de datos. En entornos empresariales donde se generan y almacenan enormes volúmenes de datos, la capacidad de SAS para procesar, administrar y analizar datos a gran escala es altamente valorada.

2. Amplio conjunto de funcionalidades: SAS ofrece un amplio conjunto de funcionalidades y técnicas estadísticas que permiten a los analistas de datos realizar una amplia gama de tareas. Desde análisis descriptivos básicos hasta análisis avanzados, modelado estadístico, minería de datos y análisis predictivo, SAS proporciona herramientas y técnicas para abordar diferentes tipos de problemas analíticos.

3. Robustez y confiabilidad: SAS es reconocido por su robustez y confiabilidad en entornos empresariales críticos. Ha estado en el mercado durante décadas y ha sido ampliamente probado y utilizado en diversas industrias. Las grandes compañías confían en SAS debido a su estabilidad, calidad y capacidad para manejar datos confidenciales y sensibles.

4. Integración con otras tecnologías: SAS se integra con otras tecnologías y sistemas, lo que facilita la interoperabilidad y el intercambio de datos con otras aplicaciones y plataformas. Esto es especialmente importante en entornos empresariales donde existen diferentes sistemas y fuentes de datos, y se requiere la integración y el intercambio de información entre ellos.

5. Soporte y comunidad de usuarios: SAS Institute proporciona servicios de soporte técnico y capacitación para ayudar a los usuarios a utilizar eficazmente el software SAS. Además, existe una comunidad de usuarios activa y en crecimiento que comparte conocimientos, mejores prácticas y soluciones a través de foros y grupos de discusión. Esto facilita el aprendizaje y el desarrollo profesional de los analistas de datos.

En resumen, SAS es utilizado y solicitado en trabajos de analista de datos en grandes compañías debido a su capacidad para manejar grandes volúmenes de datos, su amplio conjunto de funcionalidades, su robustez y confiabilidad, su integración con otras tecnologías y el soporte proporcionado por SAS Institute y la comunidad de usuarios.

## SAS y Big Data
SAS ha evolucionado para adaptarse al entorno del big data, que se caracteriza por el procesamiento y análisis de grandes volúmenes de datos de diversa naturaleza y alta velocidad. A continuación, te proporciono información sobre cómo SAS se relaciona con el big data:

1. Capacidades de procesamiento de big data: SAS ofrece soluciones que permiten el procesamiento de grandes volúmenes de datos, incluyendo tecnologías como SAS Grid Computing y SAS In-Memory Analytics. Estas tecnologías distribuyen y paralelizan las tareas de procesamiento, lo que acelera significativamente el tiempo de análisis y permite trabajar con datos a escala.

2. Integración con plataformas de big data: SAS puede integrarse con plataformas de big data como Hadoop y bases de datos NoSQL. Esto permite a los usuarios acceder y analizar datos almacenados en estos entornos, combinando el poder analítico de SAS con la capacidad de almacenamiento y procesamiento de big data de estas plataformas.

3. Análisis avanzado de big data: SAS proporciona un amplio conjunto de técnicas y algoritmos de análisis que se pueden aplicar a datos de big data. Esto incluye análisis descriptivos, modelado estadístico, análisis predictivo, minería de datos y machine learning. Con estas capacidades, los usuarios pueden descubrir patrones, identificar tendencias y realizar predicciones en grandes conjuntos de datos.

4. Visualización de datos de big data: SAS también ofrece herramientas de visualización de datos que permiten a los usuarios representar gráficamente los resultados del análisis de big data. Esto facilita la comprensión y comunicación de los hallazgos obtenidos, lo que es especialmente útil cuando se trabaja con grandes cantidades de datos.

5. Seguridad y cumplimiento normativo: SAS también se ocupa de aspectos críticos en el entorno del big data, como la seguridad y el cumplimiento normativo. Proporciona mecanismos para proteger los datos sensibles y garantizar el cumplimiento de regulaciones y políticas de privacidad.

## Conexiones con bases de datos

SAS tiene la capacidad de conectarse a una amplia variedad de fuentes de datos para recopilar información. Aquí tienes algunas de las conexiones comunes que SAS puede utilizar:

1. Bases de datos relacionales: SAS puede conectarse a bases de datos relacionales como Oracle, SQL Server, MySQL, PostgreSQL, etc. Utiliza lenguaje SQL (Structured Query Language) para extraer y manipular datos de estas bases de datos.

2. Archivos CSV: SAS puede leer y escribir archivos CSV (Comma-Separated Values), que son archivos de texto que almacenan datos tabulares en formato de valores separados por comas. Los datos se pueden importar desde archivos CSV a SAS o exportar desde SAS a archivos CSV.

3. Archivos Excel (XLSX): SAS tiene la capacidad de leer y escribir archivos Excel en formato XLSX. Esto permite importar datos de hojas de cálculo de Excel a SAS y exportar datos desde SAS a archivos Excel.

4. SAS Data Sets: SAS tiene su propio formato de archivo llamado "SAS Data Sets" o "SAS datasets". Estos archivos son creados por SAS y pueden almacenar datos de manera eficiente. SAS Data Sets se pueden leer y escribir fácilmente dentro del entorno de SAS.

5. Bases de datos NoSQL: SAS puede conectarse a bases de datos NoSQL, como MongoDB y Cassandra, que son sistemas de gestión de bases de datos diseñados para manejar grandes volúmenes de datos no estructurados o semi estructurados.

6. Microsoft Access: SAS puede conectarse a bases de datos de Microsoft Access, permitiendo la importación y exportación de datos entre SAS y Access.

7. Archivos de texto: SAS puede leer y escribir archivos de texto plano, como archivos con extensión .txt. Estos archivos contienen datos estructurados o no estructurados en un formato legible por texto, y SAS puede importar y exportar datos de estos archivos.

8. Web scraping: SAS tiene la capacidad de realizar web scraping, que implica extraer datos de páginas web en formato HTML. Puede conectarse a sitios web, analizar su estructura HTML y extraer información específica para su posterior procesamiento y análisis.

9. Conexiones ODBC y OLE DB: SAS puede conectarse a fuentes de datos a través de conexiones ODBC (Open Database Connectivity) y OLE DB (Object Linking and Embedding Database). Estas conexiones permiten la integración con una amplia variedad de sistemas de bases de datos y aplicaciones que admiten estos estándares.

10. Data Mining: SAS ofrece una amplia gama de técnicas y algoritmos de data mining, como clustering, segmentación, regresión y análisis de asociación. Puede conectarse a diferentes fuentes de datos para realizar análisis de data mining y descubrir patrones y relaciones ocultas en los datos.

11. Conexiones a través de API: SAS puede conectarse a través de API (Application Programming Interface) para recopilar datos de diversas fuentes, como redes sociales, servicios en la nube, proveedores de datos externos, entre otros. Esto permite la integración con plataformas y servicios externos para acceder y obtener datos actualizados.

Estas son solo algunas de las opciones disponibles en SAS para recopilar datos. SAS es conocido por su amplia compatibilidad con diversas fuentes de datos y su capacidad para conectarse a diferentes sistemas, lo que brinda flexibilidad en la recopilación y análisis de datos.

## Ruta de aprendizaje

1. Introducción a SAS:

- Familiarízate con el entorno de trabajo de SAS, incluyendo la interfaz gráfica y el lenguaje de programación SAS.
- Aprende a leer y escribir conjuntos de datos en SAS, así como a manipular y transformar variables.

2. Estadística descriptiva en SAS:

- Utiliza procedimientos estadísticos básicos de SAS, como PROC MEANS y PROC UNIVARIATE, para obtener medidas de resumen, distribuciones y gráficos descriptivos de tus datos.

3. Análisis de datos en SAS:

- Aprende a realizar análisis estadísticos más avanzados utilizando procedimientos como PROC REG para la regresión lineal y PROC ANOVA para el análisis de varianza.
- Explora otros procedimientos estadísticos en SAS, como PROC FREQ para tablas de frecuencia y PROC CORR para el cálculo de correlaciones.

4. Modelado estadístico en SAS:

- Aprende a realizar modelado estadístico utilizando procedimientos como PROC LOGISTIC para regresión logística y PROC GLM para análisis de varianza y covarianza.
- Explora técnicas más avanzadas, como análisis de supervivencia (PROC LIFETEST) y análisis de series de tiempo (PROC ARIMA).

5. Minería de datos en SAS:

- Utiliza técnicas de minería de datos en SAS, como el procedimiento PROC HPDM para descubrir patrones y relaciones ocultas en los datos.
- Aprende a utilizar herramientas de visualización en SAS, como PROC SGPLOT y PROC SGPANEL, para representar gráficamente los resultados del análisis.

6. Aprendizaje automático en SAS:

- Explora las capacidades de aprendizaje automático en SAS, utilizando procedimientos como PROC HPFOREST para bosques aleatorios y PROC HPSVM para máquinas de vectores de soporte.
- Aprende a evaluar y validar modelos utilizando técnicas como validación cruzada y curvas ROC.

Es importante tener en cuenta que esta es solo una ruta general y que el aprendizaje continuo y la práctica son fundamentales para dominar SAS y el análisis estadístico. SAS ofrece una amplia documentación, recursos de capacitación y ejemplos de código que pueden ser de gran ayuda en tu proceso de aprendizaje.

## Automatizar procesos con SAS
Sí, puedes automatizar procesos en SAS utilizando diferentes métodos y técnicas. Automatizar procesos en SAS te permite ahorrar tiempo y esfuerzo al ejecutar tareas repetitivas o secuencias de comandos.

Aquí tienes algunas opciones para automatizar procesos en SAS:

1. Macros SAS: Las macros SAS son una forma de automatizar tareas utilizando código SAS. Puedes definir macros con parámetros y luego llamarlas desde tus programas SAS. Las macros te permiten escribir código reusable y dinámico, lo que facilita la automatización de tareas repetitivas.

2. Programación por lotes: Puedes utilizar archivos de comandos por lotes en SAS para ejecutar secuencias de comandos automáticamente. Un archivo de comandos por lotes contiene una serie de instrucciones SAS que se ejecutan en secuencia. Puedes programar la ejecución del archivo de comandos por lotes en un horario específico o en respuesta a ciertos eventos.

3. Programación por programación por línea de comandos: Puedes utilizar la línea de comandos de SAS para ejecutar comandos y programas SAS directamente desde la línea de comandos del sistema operativo. Esto te permite automatizar la ejecución de programas SAS utilizando scripts o programación en otros lenguajes.

4. Scheduler SAS: SAS ofrece un componente llamado SAS Scheduler, que es una herramienta para la automatización de tareas en SAS. Puedes programar la ejecución de programas SAS, definir dependencias entre tareas y administrar el flujo de trabajo en un entorno automatizado.

Estas son solo algunas opciones para automatizar procesos en SAS. El enfoque que elijas dependerá de tus necesidades específicas y del entorno en el que estés trabajando. SAS ofrece una amplia documentación y recursos que te ayudarán a aprender más sobre la automatización en SAS y a aprovechar al máximo estas capacidades.

------------------------------------------

### Ejemplo de Macro SAS
Supongamos que tienes un conjunto de datos que contiene información de ventas mensuales y quieres calcular el promedio de ventas para cada año. Puedes utilizar una macro para automatizar este cálculo:

```SAS
/* Definición de la macro */
%macro calcular_promedio_ventas(datos);
   /* Paso 1: Leer el conjunto de datos */
   data ventas;
      set &datos;
   run;

   /* Paso 2: Calcular el promedio de ventas para cada año */
   proc means data=ventas noprint;
      var ventas;
      class year;
      output out=resultado mean=promedio_ventas;
   run;

   /* Paso 3: Mostrar los resultados */
   proc print data=resultado;
   run;
%mend;

/* Uso de la macro */
%calcular_promedio_ventas(tus_datos);
```

En este ejemplo, la macro se llama "calcular_promedio_ventas" y acepta un parámetro llamado "datos", que representa el nombre del conjunto de datos que deseas utilizar. La macro realiza los siguientes pasos:

1. Lee el conjunto de datos especificado por el parámetro "datos" utilizando la declaración SET.
2. Calcula el promedio de ventas para cada año utilizando el procedimiento MEANS. Los resultados se almacenan en un nuevo conjunto de datos llamado "resultado".
3. Muestra los resultados utilizando el procedimiento PRINT.

Para utilizar la macro, debes llamarla utilizando %calcular_promedio_ventas(tus_datos), donde "tus_datos" es el nombre de tu conjunto de datos.

### Ejemplo de un análisis estadístico 

Aquí tienes un ejemplo básico de cómo realizar un análisis estadístico descriptivo utilizando SAS y una base de datos SQL que contiene registros de estados financieros:
```SAS
/* Paso 1: Conectarse a la base de datos SQL */
libname mylib sqlsvr user=myusername password=mypassword server=myserver database=mydatabase;

/* Paso 2: Leer los datos financieros desde la base de datos */
data financials;
    set mylib.financial_table; /* Reemplaza "financial_table" con el nombre de la tabla que contiene los datos financieros */
run;

/* Paso 3: Realizar un análisis estadístico descriptivo */
proc means data=financials;
    var revenue profit assets; /* Reemplaza con las variables de interés */
run;
```
En este ejemplo, primero nos conectamos a la base de datos SQL utilizando la instrucción libname y proporcionando los detalles de inicio de sesión y la ubicación del servidor. Luego, utilizamos la instrucción data para leer los datos financieros desde la tabla específica en la base de datos.

Después, usamos el procedimiento proc means para realizar el análisis estadístico descriptivo. En el ejemplo, se seleccionan las variables de interés (revenue, profit y assets) y se calculan estadísticas descriptivas como la media, la mediana, el mínimo, el máximo y la desviación estándar.

Puedes personalizar este código según tus necesidades. Asegúrate de reemplazar "mylib.financial_table" con el nombre de tu tabla de datos financieros y ajustar las variables de interés según los campos presentes en tu base de datos.

**Exportamos el archivo a Excel para generar una presentación:**
```SAS
/* Paso 4: Exportar los datos a un archivo de Excel */
proc export data=financials
    outfile='ruta/del/archivo.xlsx'
    dbms=xlsx
    replace;
    sheet='Datos financieros';
run;
```
En este ejemplo, debes reemplazar 'ruta/del/archivo.xlsx' con la ubicación y el nombre de archivo deseado para el archivo de Excel que se generará.

El procedimiento proc export exporta los datos de la tabla financials al archivo de Excel especificado. El argumento dbms=xlsx indica que el formato de salida es Excel (.xlsx). Puedes cambiarlo a dbms=xls si deseas un archivo de Excel en formato anterior (.xls).

Además, puedes especificar el nombre de la hoja de Excel con el argumento sheet. En el ejemplo, se utiliza 'Datos financieros', pero puedes cambiarlo según tus preferencias.

Una vez que hayas exportado los datos a un archivo de Excel, podrás utilizar ese archivo como origen de datos en Power BI para crear tus visualizaciones y presentaciones a tus superiores.

Recuerda que debes tener instalado Microsoft Excel en el equipo donde se ejecuta SAS para que funcione la exportación a formato Excel.

----------------------------------------

### Ejemplo avanzado

Ahora quiero extraer tres columnas de una tabla desde sql las cuales he identificado que son variables explicativas para el valor de un activo y hacer una regresion lineal multple para predecir por ejemplo el valor de un activo

Si deseas realizar una regresión lineal múltiple en SAS utilizando tres columnas de una tabla en SQL como variables explicativas para predecir el valor de un activo, puedes seguir los siguientes pasos:

1. Conéctate a la base de datos SQL utilizando la instrucción LIBNAME en SAS. Asegúrate de proporcionar los detalles de inicio de sesión, servidor y base de datos correctos.
```SAS
libname mylib sqlsvr user=myusername password=mypassword server=myserver database=mydatabase;

```
2. Crea un conjunto de datos SAS utilizando el procedimiento PROC SQL para extraer las tres columnas de la tabla SQL que deseas utilizar como variables explicativas y el valor del activo que deseas predecir. Puedes utilizar una consulta SQL para seleccionar las columnas específicas y aplicar cualquier filtro o condición necesario.

```SAS
proc sql;
    create table mydata as
    select columna1, columna2, columna3, valor_activo
    from mylib.mytable; /* Reemplaza "mytable" con el nombre de la tabla en la base de datos SQL */
quit;
```
3. Utiliza el procedimiento PROC REG de SAS para realizar la regresión lineal múltiple utilizando las tres columnas como variables explicativas y el valor del activo como variable objetivo.
```SAS
proc reg data=mydata;
    model valor_activo = columna1 columna2 columna3;
run;
```
En este ejemplo, debes reemplazar "columna1", "columna2" y "columna3" con los nombres de las tres columnas que has identificado como variables explicativas, y "valor_activo" con el nombre de la columna que contiene los valores del activo.

El procedimiento PROC REG realizará la regresión lineal múltiple y proporcionará los resultados, incluyendo los coeficientes, estadísticas de ajuste, valores p, etc.

Recuerda ajustar los nombres de la base de datos, la tabla y las columnas según tu entorno y configuración específica. Además, asegúrate de tener los permisos adecuados para acceder y leer la tabla desde la base de datos SQL en SAS.

Ten en cuenta que este es un ejemplo básico de una regresión lineal múltiple en SAS. Puedes explorar opciones adicionales en SAS para analizar y validar el modelo de regresión, como la evaluación de la calidad del ajuste, la detección de multicolinealidad, la selección de variables, entre otros.

-----------------------

### Ejemplo intermedionde consultas

Quiero agrupar por una columna 3 registros que están en 3 diferentes tablas de SQL con SAS, ¿Como podría hacerlo? Por ejemplo es empleado_id en una tabla tengo sus datos personales, en otra tabla tengo su informacion laboral: departamento, ubicacion, salario y en otra tabla hay registros de sus proyectos. Debo presentar en una tabla todos los datos relacionados a el empleado, **las tres tablas se relaciona por la clave empleado_id**.

Para agrupar registros de tres tablas diferentes en SAS, utilizando la clave `empleado_id` como relación, puedes seguir estos pasos:

1. Conectarse a la base de datos SQL utilizando la instrucción `LIBNAME` en SAS. Asegúrate de proporcionar los detalles de inicio de sesión, servidor y base de datos correctos.

```sas
libname mylib sqlsvr user=myusername password=mypassword server=myserver database=mydatabase;
```

2. Utiliza el procedimiento `PROC SQL` para realizar una combinación (join) de las tres tablas utilizando la clave `empleado_id`.

```sas
proc sql;
    create table datos_completos as
    select a.empleado_id, a.nombre, a.apellido, b.departamento, b.ubicacion, b.salario, c.proyecto
    from mylib.tabla_personal a
    left join mylib.tabla_laboral b
    on a.empleado_id = b.empleado_id
    left join mylib.tabla_proyectos c
    on a.empleado_id = c.empleado_id;
quit;
```

En este ejemplo, debes reemplazar "mylib" con la librería en la que se encuentran las tablas en la base de datos SQL. Además, reemplaza "tabla_personal", "tabla_laboral" y "tabla_proyectos" con los nombres reales de las tablas.

Utilizamos el operador `LEFT JOIN` para asegurarnos de que todos los registros de la tabla personal se incluyan en el resultado final, incluso si no tienen coincidencias en las otras tablas.

3. La salida de esta consulta se almacenará en una nueva tabla llamada "datos_completos". Esta tabla contendrá todos los datos relacionados al empleado, incluyendo sus datos personales, información laboral y registros de proyectos.

Ahora puedes utilizar la tabla "datos_completos" para realizar análisis, generar informes o realizar cualquier operación adicional que necesites.

Recuerda que debes ajustar los nombres de la base de datos, las tablas y las columnas según tu entorno y configuración específica. Además, asegúrate de tener los permisos adecuados para acceder y leer las tablas desde la base de datos SQL en SAS.
